export const enum LandingPage {
    agency,
    collection,
    home,
}

export const enum ProductInfo {
    description,
    materials,
}

export const enum NavDirection {
    prev,
    next,
}